# Google Search Console verification token, from the "HTML tag" method —
# the content="..." value only, not the whole <meta> tag.
# Unset means no verification tag is emitted, which is the correct default.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
